# .env.example
# ============================================
# 🌍 CẤU HÌNH SERVER & MÔI TRƯỜNG
# ============================================

# Port mà server sẽ chạy
PORT=5000

# Môi trường hoạt động: development, production, test
NODE_ENV=development

# Domain được phép truy cập API (CORS)
CORS_ORIGIN=http://localhost:3000

# ============================================
# 🗄️ CƠ SỞ DỮ LIỆU MONGODB
# ============================================

# Chuỗi kết nối MongoDB
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# ============================================
# 🔐 CẤU HÌNH JWT TOKEN
# ============================================

# Secret key để ký Access Token
JWT_ACCESS_SECRET=access_secret_key_cua_ban_o_day

# Secret key để ký Refresh Token  
JWT_REFRESH_SECRET=refresh_secret_key_cua_ban_o_day

# Thời gian hết hạn Access Token
ACCESS_TOKEN_EXPIRES_IN=15m

# Thời gian hết hạn Refresh Token
REFRESH_TOKEN_EXPIRES_IN=7d

# ============================================
# 🧂 BẢO MẬT MẬT KHẨU (BCRYPT)
# ============================================

# Số vòng mã hóa bcrypt
SALT_ROUNDS=12

# ============================================
# 📧 CẤU HÌNH EMAIL / SMTP
# ============================================

# Email người gửi mặc định
EMAIL_FROM=no-reply@domain-cua-ban.com

# Máy chủ SMTP
SMTP_HOST=smtp.email-service.com

# Cổng SMTP
SMTP_PORT=587

# Tên đăng nhập SMTP
SMTP_USER=ten_dang_nhap_smtp_cua_ban

# Mật khẩu SMTP
SMTP_PASS=mat_khau_smtp_cua_ban

# ============================================
# 🔒 CẤU HÌNH BẢO MẬT
# ============================================

# Tên cookie bảo vệ CSRF
CSRF_COOKIE_NAME=XSRF-TOKEN

# Số lần đăng nhập sai tối đa trước khi khóa
MAX_LOGIN_ATTEMPTS=5

# Thời gian khóa tài khoản sau khi đăng nhập sai
LOCK_TIME=15m

# ============================================
# 👑 TÀI KHOẢN SUPER ADMIN (TỰ ĐỘNG TẠO)
# ============================================

# Email Super Admin (tự tạo khi chạy lần đầu)
SUPER_ADMIN_EMAIL=admin@domain-cua-ban.com

# Mật khẩu Super Admin
SUPER_ADMIN_PASSWORD=ThayDoiMatKhauNay123!

# Tên hiển thị Super Admin
SUPER_ADMIN_NAME=Quản Trị Viên Hệ Thống

# ============================================
# 🧾 CẤU HÌNH LOGGING & AUDIT TRAIL
# ============================================

# Mức độ log: error, warn, info, debug, trace
LOG_LEVEL=info

# Bật/tắt ghi log kiểm tra
ENABLE_AUDIT_LOG=true

# Số ngày lưu trữ log trước khi tự xóa
AUDIT_LOG_RETENTION_DAYS=90

# ============================================
# 💡 HƯỚNG DẪN SỬ DỤNG
# ============================================

# 1. Sao chép file này thành .env
# 2. Điền thông tin thực của bạn vào các giá trị
# 3. KHÔNG BAO GIỜ commit file .env lên GitHub
# 4. File .env thật phải được thêm vào .gitignore